/* config.h 的 config.h 中。 由 configure 从 config.h.in 生成。 */
/* config.h.in。 由 autoheader 从 configure.ac 生成。 */

/* 如果您有 <dlfcn.h> 头文件，则定义为 1。*/
#define HAVE_DLFCN_H 1

/* 如果 fseeko（可能还有 ftello）存在并被声明，则定义为 1。*/
#define HAVE_FSEEKO 1

/* 如果您有 <inttypes.h> 头文件，则定义为 1。*/
#define HAVE_INTTYPES_H 1

/* 如果您有 'bz2' 库 （-lbz2），则定义为 1。*/
#define HAVE_LIBBZ2 1

/* 如果您有 'z' 库 （-lz），则定义为 1。*/
#define HAVE_LIBZ 1

/* 如果您有 <memory.h> 头文件，则定义为 1。*/
#define HAVE_MEMORY_H 1

/* 如果您有 <stdint.h> 头文件，则定义为 1。*/
#define HAVE_STDINT_H 1

/* 如果您有 <stdlib.h> 头文件，则定义为 1。
#define HAVE_STDLIB_H 1

/* 如果您有 <strings.h> 头文件，则定义为 1。*/
#define HAVE_STRINGS_H 1

/* 如果您有 <string.h> 头文件，则定义为 1。*/
#define HAVE_STRING_H 1
/* 如果您有 <sys/stat.h> 头文件，则定义为 1。*/
#define HAVE_SYS_STAT_H 1

/* 如果您有 <sys/types.h> 头文件，则定义为 1。*/
#define HAVE_SYS_TYPES_H 1

/* 如果您有 <unistd.h> 头文件，则定义为 1。*/
#define HAVE_UNISTD_H 1

/* 软件包名称 */
#define PACKAGE “libmpq”

/* 定义此软件包的错误报告应发送到的地址。
#define PACKAGE_BUGREPORT “mbroemme@plusserver.de”

/* define 为此包的全名。*/
#define PACKAGE_NAME “libmpq”

/* Define 到这个包的全名和版本。*/
#define PACKAGE_STRING “libmpq 0.4.2”

/* 将此包的短名称定义为一个符号。*/
#define PACKAGE_TARNAME “libmpq”

/* define 到这个包的版本。*/
#define PACKAGE_VERSION “0.4.2”

/*如果您有 ANSI C 头文件，则定义为 1。*/
#define STDC_HEADERS 1

/* 软件包版本号 */
#define VERSION "0.4.2"

/* 文件偏移量中的位数，在可设置的主机上。*/
#define _FILE_OFFSET_BITS 64
/* 定义为 1 以使 fseeko 在某些主机上可见（例如 glibc 2.2）。*/
/* #undef _LARGEFILE_SOURCE */

/* 在 AIX 样式的主机上为大文件定义。*/
/* #undef _LARGE_FILES */